{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "serve": "nodemon ./bin/www --exec babel-node",
    "clean": "rm -rf dist",
    "build": "npm run clean && webpack",
    "serve-build": "npm run build && node ./dist"
  },
  "dependencies": {
    "body-parser": "~1.18.2",
    "config": "^1.30.0",
    "express": "~4.15.5",
    "mongodb": "^3.0.3",
    "mongoose": "^5.0.8"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.3.0",
    "nodemon": "^1.17.1",
    "webpack": "^4.1.1",
    "webpack-cli": "^2.0.10"
  }
}
